# 角色卡更新指南

## 快速更新步骤

当你在 `cards/` 目录下添加或删除 PNG 角色卡后，按以下步骤更新：

### 方法一：双击运行（推荐）

1. 将新的 PNG 角色卡文件放入 `cards/` 目录
2. 双击运行 `update-cards.bat`
3. 等待脚本完成
4. 刷新浏览器即可看到新角色

### 方法二：命令行运行

```bash
# 方式1：运行批处理脚本
.\update-cards.bat

# 方式2：直接运行 Python 脚本
python tmp\generate-card-metadata.py
```

## 工作原理

脚本会：
1. 扫描 `cards/` 目录下的所有 `.png` 文件
2. 解析 PNG 文件中嵌入的 SillyTavern V2 格式元数据
3. 生成两个文件：
   - `cards/cards-metadata.json` - 格式化的 JSON（便于查看）
   - `js/cards-metadata.js` - 压缩的 JS 文件（供浏览器加载）

## 支持的格式

- **SillyTavern V2 角色卡**：PNG 文件内嵌 `chara` 字段（base64 编码的 JSON）
- 文件命名无限制，但建议使用英文和下划线

## 常见问题

### Q: 运行脚本后提示 "Python 未找到"
**A**: 需要先安装 Python 3.7+，下载地址：https://www.python.org/downloads/

### Q: 新卡片没有显示
**A**: 检查：
1. PNG 文件是否正确放在 `cards/` 目录下
2. PNG 文件是否为有效的 SillyTavern V2 格式
3. 是否已刷新浏览器（Ctrl+F5 强制刷新）

### Q: 某些卡片解析失败
**A**: 可能原因：
- PNG 文件损坏
- 不是标准的 SillyTavern V2 格式
- 文件内容编码问题

脚本会跳过无法解析的文件，不影响其他卡片。

## 当前状态

最近一次更新：80 张角色卡
- 包含你新加的："女主养成"、"这些仙子都是高冷反差骚婊子！"

---

**提示**：以后每次添加新卡片，只需双击 `update-cards.bat` 即可。
