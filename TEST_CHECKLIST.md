# 测试清单

## 问题修复测试

### ✅ 问题1：公网端等待气泡不显示
**测试步骤**：
1. 打开应用，选择任意角色
2. 发送一条消息
3. 观察等待气泡是否立即显示 "💭 正在思考中…"

**预期结果**：
- 等待气泡立即显示，无论 API 响应速度快慢
- 切出去再回来，等待气泡仍然显示（如果还在等待中）

---

### ✅ 问题2：切出再切回只显示气泡，不显示流式文字
**测试步骤**：
1. 发送一条消息
2. 在流式响应开始后（约 1 秒），立即切换到其他应用
3. 等待 3-5 秒后切回应用
4. 观察是否能看到之前累积的文字
5. 观察后续文字是否能继续追加

**预期结果**：
- 切回来后能看到已累积的所有文字
- 后续流式文字能正常追加
- 不会出现"只显示空气泡"的情况

---

## 新功能测试

### ✅ 消息编辑功能
**测试步骤**：
1. 发送一条用户消息并获得 AI 回复
2. 长按用户消息（移动端）或右键（桌面端）
3. 选择"✏️ 编辑"
4. 修改消息内容，点击"保存"
5. 观察 AI 是否重新生成回复

**预期结果**：
- 编辑模态框正常弹出
- 文本框中显示原始消息内容
- 保存后，该消息及之后的所有消息被删除
- AI 自动重新生成回复

**边界情况**：
- [ ] 编辑 AI 消息时应该被禁用（菜单中不显示编辑选项）
- [ ] 点击"取消"或覆盖层关闭模态框，不做任何修改

---

### ✅ 消息重新生成功能
**测试步骤**：
1. 发送一条消息并获得 AI 回复
2. 长按 AI 消息
3. 选择"🔄 重新生成"
4. 观察 AI 是否重新生成回复

**预期结果**：
- 该 AI 消息及之后的所有消息被删除
- AI 自动重新生成回复
- 新回复的内容可能与原来不同

---

### ✅ Swipe 功能（多候选回复）

#### 测试1：生成新候选
**测试步骤**：
1. 发送一条消息并获得 AI 回复
2. 长按 AI 消息，选择"✨ 生成新候选"
3. 等待新候选生成完成
4. 观察消息下方是否显示 "2/2" 计数器

**预期结果**：
- 生成新的候选回复
- 计数器显示 "2/2"
- 显示左右切换按钮和 ➕ 按钮

#### 测试2：左右切换候选
**测试步骤**：
1. 在有多个候选的 AI 消息上左右滑动（移动端）
2. 或点击 ◀ ▶ 按钮（桌面端）
3. 观察消息内容是否切换

**预期结果**：
- 滑动或点击后，消息内容立即切换到不同的候选
- 计数器更新为当前候选的索引（如 "1/3" → "2/3"）
- 到达第一个候选时，◀ 按钮禁用
- 到达最后一个候选时，▶ 按钮禁用

#### 测试3：每个候选的思考过程独立
**测试步骤**：
1. 切换不同的候选
2. 展开"💭 思考过程"
3. 观察思考过程是否随候选变化

**预期结果**：
- 每个候选有独立的思考过程
- 切换候选时，思考过程也相应切换

---

### ✅ 提示词模板系统

#### 测试1：选择预设模板
**测试步骤**：
1. 点击聊天菜单（右上角三点）
2. 选择"📝 提示词模板"
3. 在下拉菜单中选择不同的预设格式（Alpaca, ChatML, Vicuna）
4. 观察预览区域是否更新
5. 点击"保存"

**预期结果**：
- 模态框正常打开
- 选择不同格式时，用户/助手模板和预览自动更新
- 保存后，模板设置被持久化

#### 测试2：自定义模板
**测试步骤**：
1. 打开提示词模板设置
2. 选择"自定义格式"
3. 在文本框中输入自定义模板，使用 `{{user}}` 等变量
4. 观察预览是否实时更新
5. 保存并发送消息

**预期结果**：
- 自定义模板正常工作
- 变量正确替换为实际内容
- 预览区域实时显示格式化后的结果

---

### ✅ 聊天记录导出功能

#### 测试1：导出为纯文本
**测试步骤**：
1. 点击聊天菜单 → "💾 导出聊天"
2. 选择格式：纯文本 (.txt)
3. 勾选"包含思考过程"和"包含时间戳"
4. 点击"导出"
5. 检查下载的文件

**预期结果**：
- 文件正常下载
- 文件名格式：`聊天记录_角色名_日期.txt`
- 内容包含所有消息、时间戳、思考过程

#### 测试2：导出为 JSON
**测试步骤**：
1. 导出为 JSON 格式
2. 用文本编辑器打开文件
3. 检查 JSON 结构是否正确

**预期结果**：
- JSON 格式正确，可以被解析
- 包含角色信息、消息数组、swipes 数据等

#### 测试3：导出为 Markdown
**测试步骤**：
1. 导出为 Markdown 格式
2. 用 Markdown 编辑器打开文件
3. 检查格式是否正确

**预期结果**：
- 标题、时间、消息使用正确的 Markdown 语法
- 代码块、列表等格式正确渲染

#### 测试4：导出为 HTML
**测试步骤**：
1. 导出为 HTML 格式
2. 用浏览器打开文件
3. 检查样式和内容

**预期结果**：
- HTML 可以在浏览器中正常显示
- 包含基本样式，阅读体验良好
- 思考过程使用折叠区域显示

---

## 性能测试

### ✅ 大量消息渲染
**测试步骤**：
1. 创建一个包含 50+ 条消息的聊天
2. 添加多个 Swipe 候选
3. 测试滚动性能
4. 测试切换候选的速度

**预期结果**：
- 页面不卡顿
- 滚动流畅
- 切换候选响应迅速（< 100ms）

---

## 兼容性测试

### ✅ 移动端
- [ ] iOS Safari
- [ ] Android Chrome
- [ ] 微信内置浏览器

### ✅ 桌面端
- [ ] Chrome
- [ ] Edge
- [ ] Firefox
- [ ] Safari

---

## 数据持久化测试

**测试步骤**：
1. 进行各种操作（编辑、Swipe、设置模板等）
2. 刷新页面
3. 检查数据是否保留

**预期结果**：
- 所有消息保留
- Swipe 候选保留
- 当前选中的候选索引保留
- 提示词模板设置保留
